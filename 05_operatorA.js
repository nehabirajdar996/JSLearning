console.log(`*************** step 1 *************`);
function squareOfWordLength(value) {
    //var wordLength = value;
    console.log(`> Length of word "${value}": ${value.length}  
>> Length of square of word "${value}": ${value.length**2} `);
    
}
squareOfWordLength("JavaScript"); 
squareOfWordLength("Google Chrome");
squareOfWordLength("Developer Smart");

console.log(`*************** step 2 *************`);
var givenString = "I am Angular Developer";
function string(){
    var strLen=givenString.length;
    var dividStr=givenString.split(" ");
    console.log(`2.1  Length of string is: ${strLen} String after division: [${dividStr}]`);
    var totalWordStr=strLen*dividStr.length;
    console.log(`2.2  Multiplication of string length "${strLen}" and total words "${dividStr.length}" available in string is: ${totalWordStr}`);

}
string();














































































































































































































































































































































































































































































































































































